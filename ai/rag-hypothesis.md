# RAG hypothesis

**If** processors need answers grounded in mixed documents and tables, **then** a retrieval-augmented interface with hard refusal on unsupported numbers is worth building.

Rules if built: every numeric claim cited or marked as model estimate; low retrieval coverage → explicit gap; separate UI badges for source vs estimate vs unknown.

Status: hypothesis. No index or eval set in-repo.
